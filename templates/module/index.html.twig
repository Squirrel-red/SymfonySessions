{% extends 'base.html.twig' %}

{% block title %}Liste des modules{% endblock %}

{% block body %}


<h1>Liste des modules</h1>

{% for module in modules %}
         {# pour afficher tous les sessions #}
          <b>{{ module }}</b> <br>

             <p>
                Catégorie : <a href="{{ path('show_category', {'id':module.category.id})}}">{{ module.category }}</a> <br>
                Nom du module : {{ module.name }}  <br>
            </p>

        {# pour afficher les modules triés sur la catégorie  #}
        {# {{ module }} ({{ module.category.name }}) </br> #}

        {# pour détailler la liste on ajoute le lien #}
        {# <a href="{{ path('show_module', {'id': module.id}) }}">{{ module }}</a> ({{ module.category.name }}) </br> #}
{% endfor %}

{% endblock %}
